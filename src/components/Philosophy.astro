---
/* src/components/Philosophy.astro */
---
<section id="philosophy" class="philosophy">
  <div class="container">
    <div class="section-header">
      <h2 class="text-gradient">Our Core Philosophy</h2>
      <p>We believe when children start right, they grow right. Our approach is built on foundational values that go beyond the gym.</p>
    </div>
    
    <div class="philosophy-grid">
      <div class="phil-card bg-glass">
        <div class="icon">01</div>
        <h3>Development Before Performance</h3>
        <p>We focus on building the right mechanics and motor skills rather than just competitive results.</p>
      </div>
      
      <div class="phil-card bg-glass active">
        <div class="icon">02</div>
        <h3>Safety Before Speed</h3>
        <p>Every movement is supervised and age-appropriate. We never rush a child's progress at the cost of safety.</p>
      </div>
      
      <div class="phil-card bg-glass">
        <div class="icon">03</div>
        <h3>Confidence Before Comparison</h3>
        <p>We foster an environment where children compete with their own potential, not with each other.</p>
      </div>
    </div>
    
    <div class="development-list">
      <div class="list-item">
        <span class="dot"></span>
        <p>Strong Habits</p>
      </div>
      <div class="list-item">
        <span class="dot"></span>
        <p>Strong Confidence</p>
      </div>
      <div class="list-item">
        <span class="dot"></span>
        <p>Strong Discipline</p>
      </div>
      <div class="list-item">
        <span class="dot"></span>
        <p>Strong Character</p>
      </div>
    </div>
  </div>
</section>

<style>
  .philosophy {
    background: radial-gradient(circle at 0% 100%, rgba(52, 90, 130, 0.1) 0%, transparent 50%); /* Use Brand Blue */
  }

  .section-header {
    text-align: center;
    max-width: 700px;
    margin: 0 auto 5rem;
  }

  h2 {
    font-size: 3.5rem;
    margin-bottom: 1.5rem;
  }

  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-bottom: 5rem;
  }

  .phil-card {
    padding: 3rem 2rem;
    transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }

  .phil-card:hover {
    transform: translateY(-10px);
    border-color: var(--color-primary);
  }

  .phil-card.active {
    border-color: var(--color-primary);
    background: rgba(247, 134, 61, 0.02); /* Brand Orange */
  }

  .icon {
    font-family: var(--font-heading);
    font-size: 3rem;
    font-weight: 800;
    color: rgba(255,255,255,0.05);
    margin-bottom: 1rem;
    line-height: 1;
  }

  h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--color-primary);
  }

  .phil-card p {
    color: var(--color-text-muted);
    font-size: 1rem;
    line-height: 1.5;
  }

  .development-list {
    display: flex;
    justify-content: center;
    gap: 4rem;
    flex-wrap: wrap;
    padding: 2rem;
    border-top: 1px solid rgba(255,255,255,0.05);
  }

  .list-item {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .dot {
    width: 8px;
    height: 8px;
    background: var(--color-primary);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--color-primary);
  }

  .list-item p {
    margin: 0;
    font-weight: 700;
    text-transform: uppercase;
    font-size: 0.9rem;
    letter-spacing: 0.1em;
  }

  @media (max-width: 900px) {
    .philosophy-grid {
      grid-template-columns: 1fr;
    }
    .development-list {
      gap: 2rem;
    }
  }
</style>
