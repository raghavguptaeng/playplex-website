---
/* src/components/Hero.astro */
---
<section class="hero">
  <div class="container hero-grid">
    <div class="hero-content">
      <span class="badge">A PlayPlex Sports Venture</span>
      <h1>Strength Starts <br/><span class="text-primary">From The Beginning</span></h1>
      <p>A structured Strength, Fitness & Sports Development Programme designed for children aged 3 to 12 years.</p>
      
      <div class="hero-actions">
        <a href="tel:+919910128756" class="btn-primary">Expert Consultation</a>
        <div class="hero-stats">
          <div class="stat">
            <span class="stat-value">3-12</span>
            <span class="stat-label">Years Old</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat">
            <span class="stat-value">60</span>
            <span class="stat-label">Min Sessions</span>
          </div>
        </div>
      </div>
    </div>
    
    <div class="hero-visual">
       <div class="floating-shape s1 float"></div>
       <div class="floating-shape s2 float" style="animation-delay: -3s;"></div>
      <div class="image-wrapper">
        <img src="/playplex-website/images/hero.png" alt="Children training at PlayFT Jr." />
        <div class="tag-floating">Safety First</div>
      </div>
      <div class="glass-card footer-card">
         <p>"Developing confidence before comparison."</p>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 8rem;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -10%;
    right: -10%;
    width: 40%;
    height: 60%;
    background: radial-gradient(circle, var(--color-primary-glow) 0%, transparent 70%);
    z-index: -1;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(247, 134, 61, 0.1); /* Brand Primary Orange at 10% opacity */
    color: var(--color-primary);
    border-radius: 99px;
    font-size: 0.8rem;
    font-weight: 700;
    text-transform: uppercase;
    margin-bottom: 2rem;
    letter-spacing: 0.05em;
  }

  h1 {
    font-size: clamp(3rem, 6vw, 5rem);
    margin-bottom: 2rem;
  }

  .text-primary {
    color: var(--color-primary);
  }

  p {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    max-width: 500px;
    margin-bottom: 3rem;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 3rem;
  }

  .hero-stats {
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .stat {
    display: flex;
    flex-direction: column;
  }

  .stat-value {
    font-size: 1.5rem;
    font-weight: 800;
    font-family: var(--font-heading);
  }

  .stat-label {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: rgba(255,255,255,0.1);
  }

  .hero-visual {
    position: relative;
  }

  .floating-shape {
    position: absolute;
    border-radius: 50%;
    z-index: 5;
    background: var(--color-primary);
    filter: blur(40px);
    opacity: 0.2;
  }

  .s1 {
    width: 150px;
    height: 150px;
    top: -50px;
    right: -50px;
    background: var(--color-secondary);
  }

  .s2 {
    width: 100px;
    height: 100px;
    bottom: 20px;
    left: -40px;
    background: var(--color-accent);
  }

  .image-wrapper {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 30px 60px rgba(0,0,0,0.5);
    border: 1px solid rgba(255,255,255,0.1);
  }

  .image-wrapper img {
    width: 100%;
    display: block;
    aspect-ratio: 4/5;
    object-fit: cover;
  }

  .tag-floating {
    position: absolute;
    top: 2rem;
    right: 2rem;
    background: var(--color-accent);
    color: #000;
    padding: 0.5rem 1rem;
    border-radius: 99px;
    font-weight: 700;
    font-size: 0.8rem;
    box-shadow: 0 10px 20px rgba(34, 211, 238, 0.3);
  }

  .glass-card {
    position: absolute;
    bottom: -2rem;
    left: -2rem;
    padding: 2rem;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-md);
    max-width: 280px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.4);
  }

  .glass-card p {
    font-size: 1rem;
    color: #fff;
    margin: 0;
    font-style: italic;
  }

  @media (max-width: 1024px) {
    .hero-grid {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 5rem;
    }
    .hero-content {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    .hero-actions {
      justify-content: center;
    }
    .hero-visual {
      max-width: 500px;
      margin: 0 auto;
    }
    .glass-card {
      left: 50%;
      transform: translateX(-50%);
      bottom: -1rem;
      width: 90%;
    }
  }
</style>
