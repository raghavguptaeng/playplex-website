# PlayFT Jr. Website

Professional, kid-friendly landing page for **PlayFT Jr.**, a structured Strength, Fitness & Sports Development Programme for children aged 3 to 12.

## 🚀 Getting Started

### 1. Installation
First, install the project dependencies:
```sh
npm install
```

### 2. Development
Start the local development server with hot-reloading:
```sh
npm run dev
```
The site will be available at `http://localhost:4321`.

## 🏗️ Production Build

To build the website for production deployment, run the following command:

```sh
npm run build
```

The output will be generated in the `dist/` directory. This folder contains the static assets and HTML files ready to be hosted on any static web host (Netlify, Vercel, S3, etc.).

### Previewing the Build
Before deploying, you can preview the production build locally:
```sh
npm run preview
```

## 📂 Project Structure

- `src/components/`: Reusable Astro components (Navbar, Hero, AgeGroups, etc.)
- `src/layouts/`: Common page layouts with global styles and background effects.
- `src/pages/`: Website routes (Main landing page is `index.astro`).
- `src/styles/`: Global CSS variables and brand theme tokens.
- `public/`: Static assets like logos, hero images, and patterns.

## 🎨 Brand Colors
The theme is strictly based on the **PlayFT Jr.** brand palette:
- **Primary (Orange)**: `#F7863D`
- **Secondary (Blue)**: `#345A82`
- **Accent (Peach)**: `#E4A276`

## � Support
For any updates or changes, please refer to the contact number linked in the CTA buttons: `+919910128756`.
